name: memorization
channels:
  - conda-forge
  - pytorch
  - defaults
dependencies:
  # Python
  - python=3.10
  
  # Core scientific computing
  - numpy
  - scipy
  
  # Plotting and visualization
  - matplotlib
  
  # Image processing
  - pillow
  
  # Progress bars
  - tqdm
  
  # Natural sorting
  - natsort
  
  # Pip (for any additional packages)
  - pip

  # PyTorch (CPU-only version)
  - pytorch
  - torchvision
  - cpuonly